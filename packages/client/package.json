{
  "name": "@company-intelligence/client",
  "version": "0.0.0",
  "description": "HTTP client for the Company Intelligence v1 contract; implements CompanyIntelligenceProvider.",
  "license": "MIT",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "types": "./src/index.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@company-intelligence/provider": "workspace:*",
    "@company-intelligence/types": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "vitest": "^2.1.8"
  }
}
