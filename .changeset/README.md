# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets). Add a
changeset for any change that should produce a version bump / changelog entry:

```bash
pnpm changeset
```
